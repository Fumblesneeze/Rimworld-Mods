## 1. shared performance contract — TDD model and discovery

- [x] 1.1 TDD RED: Add failing tests for attributed benchmark IDs, staging-owner and measured-subject package IDs, exact package groups, workload version, warm-up/sample ticks, speed, repetitions, evidence lens, optional calibration/control group, method selectors, throughput checkpoints, and invalid declarations. Retained REDs: `20260811T141021937Z-34612-09f2b09b1323478b9ca9fbee34c14312` (missing contract) and `20260811-performance-host-total-red` (missing aggregate declaration ceiling); final focused GREEN: `20260811-performance-contract-final` (28/28).
- [x] 1.2 TDD GREEN: Implement the host-safe `RimWorldDevGateway.PerformanceTesting` contract and metadata discovery/grouping on top of the accepted E2E harness. The reader discovers compiled attributes without loading the fixture assembly, exact package/lens/control semantics and published boundaries are enforced before launch, and `20260811-performance-host-final` passes 34/34 host tests.

## 2. mods/RimWorldDevGateway — Circinus compatibility adapter

- [x] 2.1 INSPECT: Retain the installed Circinus package/About metadata; `Circinus.dll` identity, product build, MVID, length, and hash; schema `1.15`; native 60/240 duty policy, 64-call adaptive threshold and maximum sample shift `8`, 2000-frame ring, 3000-patch/1000-method output caps, 7200-sample run ceiling, 30-sample partial flush, and temp/delete/move persistence behavior; exact `RunRecorder`, `Instrumenter`, `ProfilerRegistry`, `DevProfiler`, `TargetCatalogue`, `ProfileTarget`, and `RunDocument` declaring types/member kinds/accessibility/parameter/return shapes; exact `CircinusSettings` field types and required values; and prompt-version constants without editing Workshop content. The retained facts and DPA comparison are recorded in `design.md` (2026-08-11).
- [x] 2.2 TDD RED: Add absent, inactive, supported-shape, changed-shape/schema, sharing-or-automatic-mode, duplicate-registration, stale-selector, native-cap overflow, persisted-document mismatch, non-empty patch/method-row correlation, empty/uninvoked no-row retention, and lifecycle-cleanup tests for the reflection adapter. Retained REDs include `20260811-circinus-adapter-red`, `20260811-circinus-correlation-red`, `20260811-circinus-poststart-cleanup-red`, `20260811-circinus-arm-ownership-red`, and `20260811-circinus-review-red`; final focused GREEN is `20260811T152452151Z-56088-d8963d39c8f74b38bfed97650b24f3a1` (23/23).
- [x] 2.3 TDD GREEN: Implement exact package/assembly/schema/member guards, isolated local-only Circinus settings, deterministic hand-armed method registration, controlled recording/markers/stop, dual raw-JSON capture, and run-owned cleanup with no hard optional reference. The installed Workshop binary passes the exact runner-backed identity/shape guard in `20260811T152348204Z-50820-adaa9e04470c4cc19814dd9f988b21f3` (1/1); the reviewed product build has zero warnings and the independent re-review found no P1/P2 issues. In-game calibration remains 2.4.
- [x] 2.4 IN-GAME: In a minimal calibration process, register one known Gateway method, exercise it through an ordinary request/game workflow, and collect its Circinus method/patch metric, mod attribution, time series, raw persisted run, duty policy, and Gateway control metrics. Exact reviewed run `artifacts/PerformanceRuns/20260811T185402449Z` loaded Harmony/Core/Circinus/Gateway, used native pause/unpause at normal speed, captured the visible map before tick 124 and after the exact 600-tick sample boundary at tick 724, and was personally inspected. Circinus retained 10 time-series samples, the hand-armed `GatewayGameControlController.Capture` method row, the exact Gateway-owned Harmony tick-boundary patch row, Gateway mod-cost attribution, native duty/adaptive policy, byte-identical in-memory/persisted raw JSON, and GC/memory/wall/tick controls. The log contained no errors and only the intentional dev-only Gateway warning; staging, credentials, exact PID, normal ModsConfig/Prefs, and normal Circinus settings all cleaned/restored. Independent final review found no P1/P2 issue.

## 3. mods/RimWorldDevGateway — dynamic method selection and metrics

- [x] 3.1 TDD RED: Add failing tests for attached Harmony-owner discovery, product tick/component overrides, explicit method/type selectors, deterministic de-duplication, unsupported generic/compiler-generated members, categories, and retained identities. Retained REDs: `20260811-performance-selection-red` (missing selector implementation), `20260811-performance-normalization-red` (missing normalizer), `20260811-performance-review-red` (transpiler collision and fail-open native measurements), `20260811-performance-review-round2-red` (lens/flag/statistical semantics), and `20260811-performance-review-round3-red` (one-way row ownership).
- [x] 3.2 TDD GREEN: Implement bounded method-set discovery and hand-armed registration plus untouched Circinus JSON; a pre-stop `DevProfiler` sidecar for method identity, hand-armed state, sample shift, calls/timed calls, empty state, and cycles; exact non-empty correlation to `PatchStat` or `MethodStat`; retained empty/uninvoked no-row reasons; and normalized total, mean, maximum, gross/shared/skip-capable/ambiguous frame-share, TPS/FPS/frame/tick/heap samples, GC/memory checkpoints, unit, denominator, workload, profiler-policy, evidence-lens, and control-mode capture. Final focused GREEN `20260811-performance-review-round3-green` passes 52/52; installed Circinus/Harmony shape checks pass 2/2; the reviewed Gateway build has zero warnings; and independent re-review found no P1/P2 issues.
- [x] 3.3 REVIEW: Measure and document Circinus duty/adaptive limits using fresh instrumented, armed-disabled-wrapper, and fully disarmed-recorder controls; separate active-timing, wrapper, and total method-instrumentation overhead without claiming an analyzer-absent control; and keep whole-assembly profiling explicit. Historical reviewed run `artifacts/PerformanceRuns/20260811T202256042Z` used three fresh minimized processes with the then-current normalized empty-map calibration, 32,768 naturally ticked calibration components, and exact 300/3,000-tick windows. That run predates the current stochastic contract and is retained only as Circinus-mechanics evidence, not as the model for gameplay workloads. The current fixtures no longer write global random or ID state. The instrumented sidecar's `24,150,016` calls versus `520,832` timed calls and final shift `1` prove native adaptive sampling. `docs/PerformanceBenchmarking.md` records active-timing `+966.9260 ms`, wrapper `+12,220.2038 ms`, total instrumentation `+13,187.1298 ms`, native policy/limits, and explicit synthetic-calibration/no-statistical/no-analyzer-absent/whole-assembly caveats. Focused CLI metadata RED `20260811-calibration-v4-metadata-red` failed 1/5 on the stale speed/selector contract; corrected GREEN `20260811T203736316Z-22628-a78457948e5a43d59820eb2b3a216534` passes 5/5.
- [x] 3.4 INSPECT/TDD RED: Retain DPA's exact installed package/assembly identity and registration/start/stop/snapshot/cleanup shape, then add absent/inactive/changed-shape, bounded-selector, raw-snapshot, and run-owned-cleanup tests for the separate diagnostic adapter. The initial focused RED failed to compile because the DPA binding/capture model was absent. Final GREEN passes 9/9 lifecycle tests plus 1/1 exact installed-binary shape test and a warning-free Gateway Release build. The guarded adapter now reflects DPA's real one-outer/raw-inner model, owns and verifies the category-specific native measurement-cycle patches, rejects a run with no native cycle, bounds assembly/selector/raw metadata, labels serialized output diagnostic-only, and retains retryable async-cleanup ownership. The final independent review found no P1/P2 issues.
- [x] 3.5 TDD GREEN/IN-GAME: Implement `-DiagnosticProfiler Dpa`; in a fresh DPA-without-Circinus process exercise one exact internal-call or transpiled-IL selector through the native workload; retain the raw diagnostic result; and prove it cannot create, satisfy, or contaminate a Circinus baseline. Focused final GREEN passes 22/22 host and 56/56 Gateway tests plus a warning-free Gateway Release build; independent final review found no P1/P2 issue. Exact reviewed run `artifacts/PerformanceRuns/DpaTask35Acceptance/20260811T225609964Z` loaded only Harmony/Core/DPA/Gateway in a minimized, background, muted, developer-mode 1600x900 window. The native unpause/sample/pause workflow visibly advanced the personally inspected map from 6h to 7h and exactly tick 300 to 3300. DPA resolved requested `Verse.Map::MapPreTick()` to its Assembly-CSharp method identity and retained 10 raw inner-callee entries with 15,999 ring samples. The raw document is diagnostic and baseline-ineligible; no Circinus, normalized, current-snapshot, comparison, or candidate artifact exists. Logs contain no error/exception entry and only the intentional dev-only Gateway warning; the DPA cleanup finalized, the E2E process was untainted, stage/process/credentials cleaned, no RimWorld process remained, and normal ModsConfig/Prefs/Circinus-settings identities were unchanged.

## 4. host runner — CLI TDD, reports, and baselines

- [x] 4.1 TDD RED: Add a real dry-run helper and failing command tests for benchmark/group filters, sample overrides, exact process plans, repetitions, table/JSON output, raw/normalized JSON, CSV/Markdown reports, exit codes, and cleanup. The retained focused RED `20260811T184224210Z-44116-072d82137edb41a784bf7ed9d7637811` exposed that the calibration plan still used a speed too short for Circinus time-series emission; earlier command/runner REDs drove the missing real-run path. Reviewed focused GREEN `20260811T185316581Z-55404-ffd5752733a9409ba24a788191e5273c` passes 67/67 across runner, runtime, normalization, cleanup, and exact plan behavior.
- [x] 4.2 TDD GREEN: Implement `scripts/Invoke-RimWorldPerformanceTests.ps1` using the E2E launcher/grouping/staging substrate. The runner now performs bounded discovery/build, pre-mutation durable staging, exact isolated minimized background launches, product deployment, local-only Circinus settings, exact tick windows, artifact normalization/copying, report aggregation, cleanup, and normal-settings/config identity checks. Exact reviewed execution `artifacts/PerformanceRuns/20260811T185402449Z` exited zero and cleaned its complete lease/process/config state; focused host/staging GREEN `20260811T184544905Z-60152-6fe213641f4f4d7196b0a5f8536920aa` plus final independent review found no P1/P2 issue.
- [x] 4.3 TDD RED/GREEN: Implement compatible-baseline selection, per-metric absolute/relative thresholds, missing/unit/workload drift failures, informational cross-version/no-threshold diffs, and explicit non-overwriting baseline candidates. The stochastic schema validates every retained repetition and its distribution, represents zero-call per-call repetitions as null with zero weight, retains an explicit 0/1 observation-rate vector even when every repetition is unobserved, pools observed time/calls without inventing a zero-cost call, validates scope-appropriate native-bounded sampling context before applying a profiled threshold, and writes validated atomic no-replace candidates. Missing reviewed thresholds are informational; explicit exceeded thresholds still fail.

## 5. performance fixtures — representative Immersive Chefs workloads

- [x] 5.1 SCENARIO CONTRACT: Define a representative 36-pawn/24-animal multi-room/pathing/kitchen/dishwashing/microwave/patient/nurse/caravan workload, broad facility/population checks, and minimum activity checkpoints. Earlier deterministic sample-start/terminal hashes and passive-control evidence are retained only as historical artifacts; they are not the current acceptance model.
- [x] 5.2 STOCHASTIC AVERAGING: Removed performance-fixture writes to global Rand and `UniqueIDsManager`, exact post-warm-up normalization, fixed wait-job controls, and runner rejection of healthy repetition state drift. The normal command now selects instrumented benchmarks only unless `-IncludeProfilerControls` or an exact control ID is requested, and retains every raw repetition with count, arithmetic mean, minimum, maximum, and sample standard deviation.
  - [x] 5.2a TDD: Focused Gateway/host tests prove healthy manifest drift is accepted, missing health evidence and broken/idle checkpoints still fail, compatibility no longer contains seeds/generated IDs/exact state hashes, default selection omits one-shot calibration but retains every ordinary three-run instrumented workload, and a requested calibration family must remain complete. A candidate round-trip preserves the raw `10,20,30` observations plus count `3`, mean `20`, min `10`, max `30`, and sample deviation `10`. Per-call coverage retains a zero-call repetition as null/weight zero beside `10@2` and `20@4`, yielding the honest pooled value `16.67`; an all-zero-call selector retains an explicit `0,0,0` observation-rate vector; malformed distributions fail validation. Product-absent controls remain explicit independent diagnostics and are not array-index paired into synthetic per-run deltas.
  - [x] 5.2b IN-GAME: Exact reviewed run `artifacts/PerformanceRuns/PragmaticAverage-20260812T162202700Z/20260812T142202823Z` completed three fresh instrumented base-colony processes with normal autonomous RimWorld behavior in 264.6 seconds. The acting agent inspected all three before/after frame pairs: each visibly advanced 6h→10h while independently generated pawns redistributed through kitchen, dining, hospital, animal, and outdoor areas; the end states visibly differed rather than being normalized. Each process advanced exactly 12,000 native ticks, completed the kitchen/dishwasher/microwave/patient/animal/caravan checkpoints, and cleaned its process/stage/settings state. All three full Player logs had zero `Error`, `Exception`, or `Warning` matches. The non-overwriting candidate retains 460 metrics with zero runtime errors under `stochastic-mean-with-pooled-per-call/v3`; for example elapsed wall time was `33808.5069, 34014.0858, 33700.0001 ms` (mean `33840.8643`, sample deviation `159.52`) and naturally varying meals produced were `6,5,6` (mean `5.67`, sample deviation `0.58`).
- [ ] 5.3 MATRIX: Add exact Processor/Dubs, Gastronomy/Hospitality/Common Sense, variety/VNPE/material/DLC, and all-supported groups with group-specific fixture branches and checkpoints.
  - [x] 5.3a DECLARATIONS: Added instrumented declarations plus optional on-demand overhead-control declarations for four exact optional families. Focused metadata tests read the built assembly without loading it and verify package order and branch checkpoints. Ordinary historical runs select only the instrumented declaration and its fresh repetitions; calibration controls are not a prerequisite for each family.
  - [x] 5.3b PROCESSOR/DUBS: Exercise both native Processor dishwasher types on one supplied Dubs network and retain water/cycle checkpoints. Exact reviewed runs `artifacts/PerformanceRuns/ProcessorDubsFinalInstrumented/20260812T082126473Z` and `artifacts/PerformanceRuns/ProcessorDubsFinalControls/20260812T082757915Z` completed all nine fresh processes across instrumented, armed-disabled, and fully-disarmed lenses with exact Harmony/Core/Circinus/Processor Framework/Dubs Bad Hygiene/Immersive Chefs/Gateway order. Every process retained branch SHA-256 `880f6d5161a1a24af5627108d57a7b27f25e50ff00513ca118b5a53bce666d3e` and full sample-start SHA-256 `160b065578a5148efa18d4b4ea48e2c226af923da014f9378272740911b1b76f`, advanced exactly 12,000 native ticks, observed the same exact dirty Thing identity inside and then clean outside each native Processor holder, completed at least one domestic and one industrial cycle, and debited 2.075–2.325 L from their one supplied Dubs network. Instrumented repetitions retained 420 cycles and positive compatible windows; both disabled lenses retained zero cycles/window and `sampled=false`. The acting agent personally inspected representative before/after native frames from every lens: the identical five-zone colony visibly advanced from 6h to 10h while cleaners, cooks, diners, nurses, patients, animals, meals, and ware redistributed through the ordinary workload. All nine logs contained zero error/fatal entries and only the intentional unrestricted-Gateway warning; configuration, local-only Circinus settings, stages, credentials, and exact processes cleaned. The retained non-overwriting candidates contain zero runtime aggregation errors after the exact-guarded idle skip-capable-patch schema correction, and independent review found no remaining P1/P2 issue.
  - [ ] 5.3c GUEST SERVICE: Exercise native Hospitality/Gastronomy waiter service and Common Sense clearing arbitration with exact ware ownership.
  - [ ] 5.3d VARIETY/VNPE/MATERIAL/DLC: Exercise public ingredient provenance through native VNPE output, modded material ware, and Royalty/Biotech expectation paths.
  - [ ] 5.3e ALL-SUPPORTED UNION: Run every constituent branch together without duplicate lifecycle ownership.
- [ ] 5.4 IN-GAME: Run the reviewed base and at least one optional-mod performance group, inspect Circinus' live hand-armed/recording state and ordinary active colony behavior, and retain compatible raw local-only reports.

## 6. docs and release gates — review and historical use

- [ ] 6.1 DOCUMENT: Update README, Gateway/development/testing docs, AGENTS, and the repo-local RimWorld skill with Circinus setup, local-only settings, commands, metrics, native caps/sampling limitations, exact matrices, active-timing/wrapper/total-method-instrumentation controls, the automated noncanonical DPA diagnostic command, fixture authoring, and baseline review.
- [ ] 6.2 REGRESSION: Run focused/complete host suites, Release/package checks, E2E regression, strict OpenSpec validation, and prove Circinus/DPA/performance-test assemblies never enter product packages.
- [ ] 6.3 REVIEW: Independently review Circinus compatibility, native sampling/instrumentation bias, local-only operation, workload realism, metric units, comparison honesty, DPA diagnostic separation, cleanup, and generic future-mod extensibility; resolve findings and rerun affected benchmarks.
