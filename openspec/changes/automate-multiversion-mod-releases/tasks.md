## 1. mods/RimWorldDevGateway — Contracts and RED tests

- [ ] 1.1 Add RED unit tests for strict target-catalog parsing, unknown fields, duplicate target IDs, invalid depot/manifest identities, invalid derived C# symbols, and missing approved file hashes.
- [ ] 1.2 Add RED unit tests for per-mod release manifests, including package/About disagreement, undeclared development/supported targets, duplicate package IDs or compatibility folders, invalid required/optional dependency identities, missing Workshop identity, and invalid file/XML-override policies.
- [ ] 1.3 Add RED tests proving invalid manifests fail before downloader, build, process-launch, or publisher adapters are invoked.
- [ ] 1.4 Define versioned schemas and typed contracts for target catalogs, mod manifests, required/optional mod graphs, C# target symbols, XML projections, dependency receipts, staged candidates, presentation bundles, verification results, dry-run diffs, and publication receipts.
- [ ] 1.5 Add RED manifest and localization-policy tests for explicit distributable/development-only classification, the six baseline languages, canonical keyed/Def/runtime inventories, missing/duplicate/stale entries, malformed XML, placeholder/tag drift, raw guarded UI literals, and the exact Gateway exemption.

## 2. mods/RimWorldDevGateway — Contract GREEN and refactor

- [ ] 2.1 Implement the minimum strict catalog and release-manifest loaders required to make the contract RED tests green.
- [ ] 2.2 Add repository-owned `release/rimworld-targets.yaml` and one non-production current-1.6 example release manifest with no invented compatibility claims and explicit empty required/optional dependency collections.
- [ ] 2.3 Refactor validation into side-effect-free public services and keep actionable property paths in every validation error while the focused and owning suites remain green.
- [ ] 2.4 Document the target-onboarding review that turns external discovery data into pinned manifest IDs and approved `Version.txt`/managed-file hashes.
- [ ] 2.5 Add RED/GREEN projection tests proving supported versions and required mod entries in generated About metadata come only from the per-mod manifest.
- [ ] 2.6 Implement strict distribution/language manifest contracts and a side-effect-free localization inventory/validation service; require agent/human authorship evidence without adding a machine-translation facility.

## 3. mods/RimWorldDevGateway — Exact Steam acquisition TDD

- [ ] 3.1 Add RED tests around a fake depot client for normalized selection hashes, cache hits, corruption quarantine, inaccessible manifests, per-depot failure isolation, leases, and secret redaction.
- [ ] 3.2 Implement the external content-addressed compile cache and immutable file index using a narrow DepotDownloader process adapter, then make the focused tests green.
- [ ] 3.3 Add RED tests and GREEN implementation for full-install cache identities composed from ordered base/DLC manifests without mixing them with compilation entries.
- [ ] 3.4 Refactor acquisition so DepotDownloader and the documented SteamCMD diagnostic fallback produce distinct provenance and never substitute the normal Steam installation.
- [ ] 3.5 Perform an opt-in real Steam smoke run that downloads only the declared current managed-file selection, verifies approved hashes, reuses the cache on a second run, and retains a secret-free receipt.
- [ ] 3.6 Attempt one desired historical target with the owning account and record an explicit inaccessible-manifest result instead of approving a nearby version if Steam rejects it.

## 4. mods/RimWorldDevGateway — Target-isolated build and staging TDD

- [ ] 4.1 Add RED integration tests proving two fake target projections receive separate MSBuild intermediate/output roots, exactly one matching `RIMWORLD<major>_<minor>` symbol, no mismatched managed assembly set, and no two compile targets in one compatibility folder.
- [ ] 4.2 Implement compile projections and explicit per-target `Zlepper.RimWorld.ModSdk` invocations with manifest-derived symbols, make the ordinary local build resolve the development target through the same rule, then make the focused build-matrix tests green.
- [ ] 4.3 Add RED tests for merging verified target outputs, generating unified supported-version and required-dependency About metadata, and rejecting a failed, unverified, or undeclared target.
- [ ] 4.4 Add RED package-policy tests for source, symbols, caches, credentials, undeclared files, tests, and Gateway assemblies entering a product candidate.
- [ ] 4.5 Implement positive-allowlist staging, sorted path/size/SHA-256 manifests, candidate digests, and immutable leases until all staging tests are green.
- [ ] 4.6 Refactor build/stage orchestration into independently rerunnable phases and prove identical inputs either reproduce hashes or name each nondeterministic file.
- [ ] 4.7 Add a C# fixture whose unconditional feature code calls a narrow seam with a `RIMWORLD1_6` legacy implementation, and capture RED/GREEN builds against synthetic current and legacy API shapes.
- [ ] 4.8 Add RED XML projection tests for canonical pass-through, ordered legacy add/replace/remove operations, a rationale-required document replacement, selector-cardinality drift, invalid output, duplicate Def identity, and unchanged source files.
- [ ] 4.9 Implement the minimum parsed typed XML operation engine and per-target ignored projection required to make the focused XML tests green without raw text preprocessing or version source folders.
- [ ] 4.10 Refactor symbol and XML projection provenance into the target receipt and reject staged compatibility folders whose symbol, transform, About version, or manifest identity disagrees.
- [ ] 4.11 Add a policy check that permits RimWorld version directives only under the neutral `Compatibility/RimWorld/` area or in exact manifest-allowlisted files with rationale, without requiring speculative legacy branches before a demonstrated divergence.
- [ ] 4.12 Make candidate staging reject a distributable mod whose English/German/Spanish/French/ChineseSimplified/Russian keyed and Def-injected catalogs do not exactly cover its player-text inventory or whose placeholders/tags/raw guarded UI strings fail policy; retain catalog hashes in stage evidence and exempt only explicitly development-only mods.

## 5. mods/RimWorldDevGateway — Exact-version regression runner TDD

- [ ] 5.1 Add RED runner tests for exact target resolution, exact executable/data paths, matching compatibility-folder package selection, full manifest-set evidence, and refusal to fall back to the normal installation.
- [ ] 5.2 Extend the scenario and grouped E2E runners with a declared target parameter while preserving their current default and exact ordered mod-list contracts.
- [ ] 5.3 Make the runner tests green for unique savedata, exact PID/start identity, normal `ModsConfig.xml` before/after hashes, graceful cleanup, and active-cache leases.
- [ ] 5.4 Refactor common target/process evidence without weakening native-action/outcome assertions, impact-selected screenshot review, stopped-profile handling, or optional-mod gates.
- [ ] 5.5 Run the existing dry-run commands and one current-version process smoke to verify target plumbing and cleanup before attempting historical gameplay acceptance.

## 6. mods/RimWorldDevGateway — Presentation compiler TDD

- [ ] 6.1 Add RED tests for structured Workshop content, BBCode/template separation from About copy, complete concise mechanics/things/buildings inventories, manifest-derived compatibility with per-integration expected behavior plus Required Mods and Optional Mods sections, explicit empty states, exact RimWorld-line copy, a truthful final-section AI disclosure, required fields, link/tag allowlists, and the installed Steamworks SDK title/description limits.
- [ ] 6.2 Add RED renderer fixtures for sprite placement, missing/stale assets, fixed dimensions, offline-only loading, pinned fonts, and text-overflow bounds.
- [ ] 6.3 Implement deterministic BBCode compilation and HTML/CSS/Playwright rendering with pinned Chromium, local fonts, viewport, scale, and disabled animation/network access.
- [ ] 6.4 Make presentation validation green for deterministic rerenders, image dimensions/size, provenance hashes, local review HTML, and unchanged source art.
- [ ] 6.5 Refactor shared banner/feature-card templates into `release/templates/` while keeping mod-authored copy and sprite mappings under `mods/<ModName>/Release/workshop/`.
- [ ] 6.6 Personally inspect the fixture preview and generated images and record layout, readability, sprite, and overflow observations before accepting the renderer.
- [ ] 6.7 Add drift tests proving hand-authored Workshop copy cannot replace, omit, reclassify, or duplicate dependency entries supplied by the release manifest.

## 7. mods/RimWorldDevGateway — Inline-image hosting experiment

- [ ] 7.1 Define an isolated, update-only experiment against a dedicated unlisted Workshop item, with explicit user authorization, expected preview inventory, and recovery snapshot.
- [ ] 7.2 Upload additional preview fixtures, query their returned Steam CDN URLs/original names, embed them into a test description, and inspect the rendered Workshop result.
- [ ] 7.3 Test replacement/removal and a second update to determine URL stability and whether old descriptions break.
- [ ] 7.4 Decide between Steam-hosted preview URLs, a named immutable repository-owned static host, or carousel-only graphics; update this design/spec before production publisher implementation.

## 8. mods/RimWorldDevGateway — Publication state-machine RED tests

- [ ] 8.1 Define a narrow SteamUGC adapter and add RED tests for existing-item ownership, invalid/zero IDs, dry-run with no mutation, bounded metadata/content/required-item diffs, and nonce/hash binding.
- [ ] 8.2 Add RED tests for every setter failure, update-language/title/description/visibility/tags/content/metadata/preview mapping, authored change notes, update-only identity safety, and the guarantee that `CreateItem` is called only by an explicitly confirmed one-time bootstrap.
- [ ] 8.3 Add RED tests for asynchronous progress, one-operation ownership, pre-submit cancellation, polling timeout, late callback, indeterminate submission, retry, and immutable-stage lifetime.
- [ ] 8.4 Add RED tests for Steam authentication, connectivity, quota, legal-agreement, callback, wrong-item, and stale-remote-state failures with secret-free receipts.
- [ ] 8.5 Add RED tests for post-submit remote metadata/preview/required-item/change-note comparison and for the ordinary path making no subscription, reacquisition, local-product move, or gameplay launch. Keep exact-item subscription/refresh, reacquired Workshop content-manifest verification, and refusal to substitute a repository-local package only in the explicit release-tooling-change validation mode.
- [ ] 8.6 Add RED tests for asynchronous required-item addition/removal, optional-item exclusion, partial graph success, remote-state query before retry, and stale dependency confirmation.

## 9. mods/RimWorldDevGateway — Publication GREEN and refactor

- [ ] 9.1 Implement authenticated loopback-only versioned dry-run, confirm, and status contracts that validate the isolated RimWorld PID/start identity and bind the required-item graph into preflight hashes.
- [ ] 9.2 Implement the main-thread SteamUGC update-only adapter with checked setter results and asynchronous callback state until mapping tests are green.
- [ ] 9.3 Implement leased operation persistence, bounded progress, legal-agreement reporting, asynchronous required-item reconciliation, indeterminate recovery, and explicit remote-query-before-retry until lifecycle tests are green.
- [ ] 9.4 Implement remote metadata/preview/required-item/change-note verification and receipt creation as the ordinary terminal path. Keep exact-item subscription/refresh, separate Workshop-content reacquisition, and subscribed-path identity evidence behind the explicit release-tooling-change validation mode until both policy branches are green.
- [ ] 9.5 Refactor publisher code so HTTP parsing, release policy, SteamUGC calls, and evidence serialization remain narrow and independently testable while the owning suite stays green.

## 10. mods/RimWorldDevGateway — Release CLI integration

- [ ] 10.1 Add the repository-local `RimWorldReleaseTool` command surface and thin PowerShell entry point for targets, acquire, build, test, present, stage, dry-run, confirm, and verify.
- [ ] 10.2 Add integration tests proving phase receipts can resume safely, changed candidates or dependency graphs invalidate dry-run, dirty candidates cannot publish, and ordinary builds/tags never publish implicitly.
- [ ] 10.3 Add a dry-run end-to-end fixture from manifest validation through candidate/presentation review without launching RimWorld or changing Steam.
- [ ] 10.4 Update development, Gateway, testing-environment, and release documentation with bootstrap, cache, current-first version onboarding, C# seam/XML override authoring, dependency metadata, presentation review, recovery, and evidence procedures.
- [ ] 10.5 Update the repository release skill from planning guidance to tested commands only after the implemented command surface exists.

## 11. mods/RimWorldDevGateway — Builds, regression, and independent review

- [ ] 11.1 Run focused tests after every RED/GREEN/refactor slice and retain non-zero RED plus GREEN evidence; reject zero-test runs.
- [ ] 11.2 Run the owning Gateway/unit/integration suites, guarded repository suite, and Release builds, then inspect the generated Gateway and example product packages.
- [ ] 11.3 Run `openspec validate --all --strict --no-interactive` after every contract change.
- [ ] 11.4 Perform an independent scoped code review, resolve or explicitly reject every finding with evidence, and rerun all affected tests and builds.

## 12. mods/RimWorldDevGateway — Final isolated in-game verification

- [ ] 12.1 With explicit user authorization, launch a fresh isolated reviewed build against the dedicated unlisted Workshop item and bind Gateway discovery, actions, screenshots, diagnostics, and cleanup to its exact PID/start identity.
- [ ] 12.2 Perform dry-run, personally inspect the item, visibility, meaningful package/presentation/required-item diff and change note, and obtain one natural-language authorization for that release intent. Keep bound hashes and tool confirmation strings internal; require fresh authorization only for meaningful intent/scope drift or another Steam submission.
- [ ] 12.3 Observe Steam progress and terminal callbacks, personally inspect the resulting Workshop metadata, required items, optional-mod behavior descriptions, final AI disclosure, previews, served preview bytes, and change note; retain the remote-only receipt and stop without subscription/reacquisition for ordinary publication. Exercise subscribed-copy comparison only when this run explicitly validates changed release publisher/subscription/install tooling.
- [ ] 12.4 Run every impact-selected product workflow on the immutable staged candidate before publication; require native-action/outcome assertions for every run and personally inspect before/action/after screenshots only for new or materially changed player-visible scenarios. Do not duplicate those workflows after ordinary publication; subscribed-path runs are tooling-change-only validation.
- [ ] 12.5 Use the minimized Dev Gateway profile by default. Run a Gateway-free duplicate only when explicitly requested or when a recorded capability gap prevents faithful Gateway control or observation; a stopped profile remains disabled until fresh user authorization.
- [ ] 12.6 Record source/build identities, target symbols, XML projection hashes, packaged and remote dependency graphs, ordered depot/mod lists, the scenario impact map, native-action/outcome assertions, required manual screenshot reviews, remote verification, operator stop directives, exact-process zero checks, normal-configuration hashes, and cleanup in one secret-free evidence directory.
- [ ] 12.7 Check implementation tasks only after the reviewed revision's tests, package checks, live publication proof, impact-selected per-target player acceptance, required manual reviews, and cleanup evidence all exist.

## 13. mods/RimWorldDevGateway — Immersive Chefs 1.6 release bootstrap

- [x] 13.1 TDD: define and validate the clean committed single-target release descriptor, positive-allowlist staging, sorted SHA-256 package manifest, presentation hashes, and mutation-free first-publication plan. The release hardening additionally behavior-tests existing-ID inventory, receipt identity recovery/conflict, exact project staging, callback failure classes, callback identity correlation, and invalid-call rejection.
- [x] 13.2 REVIEW/GATE: independently review the bootstrap, run the complete release gates, inspect the staged package and presentation, and obtain explicit confirmation bound to the exact candidate and presentation hashes. Candidate `E90629B964218F42D086302468E010A53E82D43A169E3479DD626998172035B8`, plan `FA33543E0B0A990231B30FB6D5C1B4BCD01831E82604C3D1E2FD83D730979AFF`, and presentation hashes were reviewed before the authorized mutation.
- [x] 13.3 STEAM: create the first item through RimWorld's initialized Steam session, persist its returned identity before submission, check every setter/callback and Harmony dependency result, and retain a secret-free receipt. Public item `3782589902` and its exact title/owner/app/public visibility/description hash/tags/Harmony dependency/preview were reconciled; receipt `artifacts/Releases/fumblesneeze.immersivechefs/publication/20260813T081731254Z/publication-receipt.json` retains no token.
- [x] 13.4 SUBSCRIBER: subscribe/reacquire the exact item, compare its downloaded file manifest with the reviewed candidate, then launch RimWorld 1.6 with the subscribed copy and personally observe the declared native smoke workflow. The exact Steam path `F:\Steam\steamapps\workshop\content\294100\3782589902` matched the reviewed file manifest; the fresh `20260813T081812962Z` subscribed-copy run passed native Prioritize → cooking → Consume, and its reviewed screenshots show the plated meal and exact dirty returned plate/cutlery.
- [x] 13.5 PRESENTATION: TDD the player-facing Workshop copy, five-or-more reviewed visual feature assets, final `Author's Note`, exact Steam byte/tag policy, additional-preview URL inventory, and private-first creation policy; update the reusable release and asset skills with the proven workflow. The primary preview is not repeated inline; four asset-led mechanics cards and two behavior-led illustrations truthfully depict their adjacent copy without opaque background panels or unexplained status badges. The reviewed six-card preview sync for plan `05FBBC7A7DEE343EEDCB242FF26591374C5470A12C798FF26F7E12438B375DF9` returned exact Steam-hosted URLs whose bytes match the local hashes; the resolved description is 7,989 UTF-8 bytes, includes the linked Recommended Mods section and final two-paragraph Author's Note, and remains below Steam's 8,000-byte terminator-inclusive ceiling.
- [x] 13.6 WORKSHOP UPDATE: synchronized the six reviewed feature images to retained public item `3782589902`, then published candidate `164ECDC1632B3B1A652008EE3FCBE2677997618678F32D1ACE89D0EA2117413E` from plan `622D61C6ABB650FBD653FBF466ABD46B73468DBFB5923C4A59EDF3E2F449ABB9`. Publication receipt `artifacts/Releases/fumblesneeze.immersivechefs/publication/20260813T112744731Z/publication-receipt.json` proves the exact remote title, owner, application, public visibility, `Mod`/`1.6` tags, Harmony dependency, 7,989-byte description hash `B08944A0204012D736B83067326478832C418C19A69AD74217A23880B825F10F`, and ordered six-preview inventory. The acting agent inspected the live Steam page: the feature cards rendered cleanly without repeated title art, opaque white panels, or status checkmarks; the linked Recommended Mods section preceded the nonduplicating Optional Mods list; and the Author's Note was final. After subscribing, Steam reacquired the exact 15,094,455-byte candidate under Workshop item `3782589902`; isolated smoke run `20260813T112839880Z` used native `Prioritize` and `Consume` actions and visibly produced a plated simple meal, then returned the exact plate, cutlery, and cookware dirty. The acting agent inspected all six causal screenshots, the corrected microwave/kitchenware art, clean logs, exact-process cleanup, local-product restoration, and credential removal.

## 14. mods/RimWorldDevGateway — Incremental change notes and gameplay showcases

- [x] 14.1 CONTRACT: add strict per-mod showcase declarations and the exact Immersive Chefs inventory: Gastronomy order-to-service; Pick Up And Haul/Dubs/Processor dirty-dish batch; prisoner nutrient-paste dining in Prisoner Jumpsuits; prepared-food/fine-meal/Thermodynamics hot storage; and a memories still showing vanilla ate-without-table plus Immersive Chefs ate-without-cutlery and cold-food memories. `showcases.json` now owns the exact package chains, natural scene/workflow/beats, 1280×720 crop contracts, screenshot/GIF outputs, and unique Steam slots 5–9; focused release validation and strict OpenSpec pass.
- [x] 14.2 CAPTURE: extend the Gateway presentation capture surface with non-selecting Thing crops, camera-centered pixel rectangle/offset crops, bounded frame sequences, exact applied-rectangle/timing evidence, and state restoration; use the proven capture mode in game. Focused Gateway tests pass 49/49; exact minimized Core+Gateway run `ShowcaseCropLiveFinal-20260813T134434525Z` captured a reviewed 960×540 offset crop and 3-second GIF without selection/camera mutation, retained exact PID/start/run/crop/frame/ffmpeg/ffprobe provenance, and cleaned credentials/config/process state.
- [x] 14.3 CHANGE NOTES: make every retained-ID update reject an empty/generic/unchanged note, bind the authored note into dry-run/confirmation, submit it unchanged, and retain its verified Steam change-history result. Keep first publication's note policy separate. Release validation now compares the tracked previous note only for retained IDs; preview-only sync passes `null` to Steam, final publish submits the authored note once, remote changelog reconciliation requires a new exact entry, and the credential-free receipt retains its identity.
- [ ] 14.4 HUMAN-DEFERRED SHOWCASE SCENES: humans may build the four natural Immersive Chefs scenes with the exact optional package chains, believable rooms/materials/decor/names/apparel/storage/linking, compact action geography and ordinary native jobs/orders/memories. Agents are forbidden from arranging, repairing, capturing, promoting, or publishing these scenes. Do not reuse cleared-map regression fixtures or earlier agent attempts as publishable visuals.
  - [x] 14.4a RESEARCH/SKILL: replace the rejected 12-blueprint shortcut with a reproducible Real Ruins bulk-corpus analyzer; retain at least 5,000 metadata records, safely parse at least 2,000 blueprint bodies, report every rejection/unresolved Def, derive exact sample-sized placement statistics, visually inspect a stratified matching-layout subset, and codify the resulting rules plus rejected-draft lessons in the repo-local `rimworld-realistic-base-generation` skill. `artifacts/BaseDesignResearch/20260814-real-ruins-corpus/summary.json` retains 5,000 metadata rows, 2,200 bounded attempts, 2,199 parsed blueprints, one reported rejection, exact corpus hash/bytes and sample-sized measurements; the acting agent inspected the stratified `visual-review`, `food-colony-review`, and corrected `food-colony-review-v2` contacts. The committed analyzer-focused suite passed 5/5 after corpus/tool/skill commit `0f90dd0`; raw screenshots, contact sheets and blueprints remain ignored research inputs, and weakly attributed visuals remain ineligible for recurrent rules.
  - [ ] 14.4b RELEASE BINDING: require every declared showcase's completed design record, validate its reference/rule/adjacency/placement inventory, and hash it into the immutable release plan so guessed or stale geometry cannot pass the publication gate on an honor-system declaration alone.
- [ ] 14.5 HUMAN-DEFERRED CAPTURE/ENCODE: a human may capture and review one high-detail screenshot plus one no-longer-than-five-second GIF for each showcase, and the requested readable memories screenshot. Agents must not perform or resume this task. Any future human outputs still require tight fixed crops, retained provenance, and sub-one-MiB final previews.
  - [x] 14.5a MEMORIES STILL: In exact Harmony/Core/Immersive/Gateway process `artifacts/ShowcaseRuns/memories-accepted-20260813T160603838Z`, arrange only a finished room, a cold plated Simple meal and a conscious named diner without cutlery or a usable table; invoke the enabled native right-click `Consume simple meal` option; then visibly inspect the resulting native Needs pane. The acting agent observed `Ate without cutlery`, `Ate without table`, and `Cold meal` together over the lit wood-and-granite dining room with no selection bracket in the retained 1280x720 crop. The accepted process had no product error/exception and completed exact process, credential, configuration and package cleanup. Checked-in capture provenance binds all loadable product files, the exact crop, source frame and reviewed observation.
- [ ] 14.6 HUMAN-DEFERRED SHOWCASE PRESENTATION: after humans supply and approve the showcase media, replace or reorder those declared additional previews without repeating the primary preview and pass the normal local review, provenance, validation, and mutation gates. Agents must not perform this task unless a later explicit user decision lifts the deferral.
- [x] 14.10 NON-SHOWCASE UPDATE: encode the human deferral in the showcase manifest, release builder, immutable plan, publisher validation, root instructions and release skill; retain the six independently reviewed feature cards; exclude all showcase files/evidence; use a truthful player-facing change note; and publish/subscribe/smoke-test the existing item under the normal mutation gate. Published existing item `3782589902` from immutable plan `468D9B47F91576FDFE247CA788AADC3D110E628EB2E2DB17354D7B2A3F4E15C2` / candidate `DDD9F8EC74187CBA566454DA020817C564FFA5A107C0F4FBDEB26BCFC3EEBE0F`; Steam returned change entry `1786828839`; the subscribed Workshop copy passed the native cooking-and-dining workflow in `artifacts/Releases/fumblesneeze.immersivechefs/publication/20260815T212002245Z`, with exact dirty cookware/plate/cutlery inspection, clean logs, restored user configuration, and complete cleanup.
- [x] 14.7 IDENTITY: check in `mods/ImmersiveChefs/About/PublishedFileId.txt` with item `3782589902`; make existing-item staging require exact agreement with `release.json`, receipts, package and remote identity; retain generated-at-first-publication only as a one-transaction bootstrap state. Commit `98f88a7` retains the exact repository identity; the focused release behavior and checked-in publisher compiler tests pass, and the warning-free product build contains the same ID.
- [x] 14.8 NONINTERACTIVE TESTS: replace visible Notepad process fixtures with exact hidden noninteractive children, add a guard against unrelated desktop-app launches, and prove process/start identity plus cleanup semantics remain intact. The exact visible-editor guard and both retained PID/start-identity regressions pass with a hidden noninteractive PowerShell child.
- [x] 14.9 CARD CORNERS: record the measured Steam Workshop background color, render every rounded feature card with that exact opaque outer-corner composite, replace the obsolete transparent-corner assertion, rerender/review all cards locally, and defer Steam mutation until the complete next release dry-run is reviewed. The versioned template records the 2026-08-14 `#1b2838` observation; all six 1164x655 cards have exact opaque `#1b2838` pixels at all four corners and were inspected locally without mutating Steam.
- [x] 14.11 STUFF-COLORED FEATURE ART: add explicit material colors to Stuffable presentation entries, apply them through the exact shipped masks before card compositing, rerender the six ordinary feature cards, and review the Steel kitchenware response without touching human-deferred showcases or publishing to Steam. The presentation manifest now declares Core Steel `#696969` for every ordinary Stuffable kitchenware subject, the renderer applies the shipped Stuff masks before composition while preserving fixed handles/outlines/grime, and the affected kitchenware, dishwashing and meals cards were rerendered. The acting agent inspected `artifacts/VisualAssets/DirtyVisibility-20260816/workshop-steel-contact-v3.png`: the kitchenware reads as Steel without black boxes or translucent artifacts and the intentionally exaggerated dirt remains visible. Focused final GREEN `20260815T231928946Z-22156-58f4a314f3bf4acf970f4705eb2d6dd8` passed 51/51; human-deferred showcases and Steam were untouched.
- [x] 14.12 CERAMICS/WATER UPDATE: published reviewed source `60549613a0b7ca913391546e9884878958ae4697` and candidate `ACA7748A928391CA8258DD3CC3A28E6A16595ABA9AC915CFCC19FD1695A24433` through immutable plan `ECCD39587FCBA98683927620E0F901DFE838498113AF1D55D20D4B965E9BC516` to existing item `3782589902`. Steam change entry `1786895796` retains the exact player-facing note; remote metadata, Public visibility, Harmony dependency, `1.6` tag, 7,995-byte description, primary title image, and seven additional previews converged. Additional slot zero is the byte-matching 1280x720 `preview-main.png`, followed by the six 1164x655 feature cards; the BBCode contains only those six cards and no title-image URL; all human-deferred showcases remain excluded. The release reacquired the exact 15,036,837-byte Workshop package from `F:\Steam\steamapps\workshop\content\294100\3782589902` and launched RimWorld 1.6 with Harmony, Core, that subscribed copy, and Gateway. I inspected the exact action sequence: the native Prioritize callback started and visibly completed a simple-meal bill, the produced meal visibly held one plate, the native Consume callback visibly ingested it with cutlery, and the returned plate/cutlery inspectors showed `Cleanliness: dirty`; the same run asserted the exact cookware dirty return. The product log had no error/exception and only the expected Gateway-active warning; PID/config/credential/staging cleanup completed. Receipt: `artifacts/Releases/fumblesneeze.immersivechefs/publication/20260816T155609957Z/publication-receipt.json`.
- [x] 14.13 COOK FOR YOURSELF/DEPTH UPDATE: published reviewed source `6dbd477028729aee59ac0d26cf6e1b9fd347e111` and candidate `C67C3B6AC5DEA8C69D0434B2BBCA40688FB87BF409B83B8464BD73F2F3374245` through immutable plan `DC8B348B27C0EA26AE060C4AAFAF0A728135ECC5C4902C8AE4F0130FD962301E` to existing item `3782589902`. Steam change entry `1787098951` retains the exact Cook for Yourself/Stack Gap and directional-depth note. Remote Public metadata, `Mod`/`1.6` tags, Harmony-only required-item graph, 7,867-byte description, primary preview, and seven additional previews converged; Cook for Yourself, Pick Up And Haul, and Stack Gap remain optional/recommended metadata rather than Steam requirements, and all human-deferred showcases remain excluded. The release reacquired the exact 15,046,204-byte Workshop package and assertion-tested its unchanged native Prioritize → cooking → Consume workflow through the minimized Dev Gateway: one plated meal was produced and consumed, and the exact cookware, plate, and cutlery returned dirty. The changed Cook for Yourself self/patient scenarios and north/south cookware-depth scenario had already passed their impact-selected manual screenshot reviews on the unchanged runtime source; the subscribed smoke therefore required assertions without duplicate manual review. Normal configuration hashes matched, credentials and stages were cleaned, the temporary short-path drive alias was removed, and zero RimWorld processes remained. Receipt: `artifacts/Releases/fumblesneeze.immersivechefs/publication/20260819T003733331Z/publication-receipt.json`.
- [x] 14.14 COOKWARE ART/RELEASE WORKFLOW: published reviewed source `acd3d43e7eee1edbc2d08cd346d6f4bb3fd2ca86` and candidate `1FB3C91E6D974D5F0CA21CC120FF8630FA8060C2482B5685161FB30805AEC0DD` through immutable plan `10F0A5323F0A3E0A0D820F6CD63F0BC205065D90B03FBB9C040E5897BF66040F` to existing item `3782589902`. Steam change entry `1787150127` retains the exact stronger-outline cookware note; Public metadata, `Mod`/`1.6` tags, Harmony-only required-item graph, 7,906-byte description, primary preview, and seven additional preview slots converged. Steam intermittently omitted the URL for slot 3 during reconciliation, so the guarded reconciliation-only path required its exact index/type/filename and re-downloaded all seven previously resolved Steam CDN URLs to the reviewed hashes; the strict pre-submit path still requires every URL. The release reacquired the exact 15,024,877-byte Workshop package and assertion-tested its native Prioritize → cooking → Consume workflow through the minimized Dev Gateway: one plated meal was produced and consumed, and the exact cookware, plate, and cutlery returned dirty. The cookware art had already passed changed-impact live screenshot review and a blind visual gate; the subscribed behavior smoke therefore used assertions without redundant manual screenshot review. One meaningful release authorization now covers preview synchronization, byte-verified URL/provenance finalization, final submission, dependency reconciliation, reacquisition, and smoke verification; subscribed smoke uses a guarded short artifact root. Normal configuration, credentials, local product staging, and every exact release PID were cleaned. A subsequently launched main-worktree Thin Walls E2E process was identified by its command line and left untouched. Receipt: `artifacts/Releases/fumblesneeze.immersivechefs/publication/20260819T160004931Z/publication-receipt.json`.
- [x] 14.15 REMOTE-ONLY ORDINARY PUBLICATION: ordinary publication now stops after exact Steam identity/owner/app/visibility/metadata/description/tag/dependency/preview/change-note verification and exact-process cleanup. The root process, OpenSpec design/spec, release skill/checklist, documentation, publisher, and focused tests explicitly prohibit `SubscribeItem`, Workshop package reacquisition, local-product moves, and post-publication gameplay launches for ordinary releases; subscribed-copy comparison/smoke remains available only through the explicit release-tooling-change switch. The inventory-job restart fix was published from reviewed source `7a8d9b78c88681185ce184131f46de11ce6918fb` and candidate `AC68FB3D0D56E6DF7816974A71CE17CFC95B9FDEB39F42267E09FC02269B323E` through immutable plan `90B7AC2F32F584A7F8D9B5AA36449035F7486B824D57544CD2B781FC3769E303` to existing item `3782589902`. Steam change entry `1787218919` retains the exact inventory reheating/eating note; the remote item remained owned by the expected Steam user for app `294100`, Public, titled `Immersive Chefs`, tagged `Mod,1.6`, dependent only on Harmony, and bound to the exact plan metadata, 7,906-byte description, primary preview, and seven additional preview slots. Reconciliation used publisher revision `acdf883e1c8e090a27f7b94a01e47b71a48f394b`; its receipt records subscriber verification as not requested, with installed path and subscribed smoke null. No Workshop subscription, reacquisition, local-product move, or post-publication gameplay run occurred; the exact publisher process, token, and staging state were cleaned and zero RimWorld processes remained. Receipt: `artifacts/Releases/fumblesneeze.immersivechefs/publication/20260820T101457437Z/publication-receipt.json`.
- [x] 14.16 HONEST CHANGE-HISTORY CLIENT: retained repeated pre-admission browser-header HTTP 429s plus a request-free ten-minute cooldown, while the same endpoint returned HTTP 200 to an honest client and still exposed only the prior release note. Focused RED `Workshop_change_history_identifies_the_release_tool_instead_of_impersonating_a_browser` failed against the Chrome impersonation and passed after switching to stable `ImmersiveChefsWorkshopRelease/1.0`; the owning/release/localization gate passed `53/53`, strict OpenSpec passed `7/7`, a same-header live query returned HTTP 200, and independent review returned PASS. Published reviewed source `6cad6996ffd26c6bdb5639ddb67f8b952ed73456` and candidate `68E12FE43C8B0FB6A3CC7026A691C964A1557C41F8504E66A41AB0500AD57C6B` through plan `87AC874A3F8E4E4F6C4C55C60112895269180F79870934199C8F7F0B51ACCE6F` to existing public item `3782589902`; remote change entry `1787252207`, exact owner/app/title, `Mod,1.6` tags, Harmony-only dependency, 7,967-byte description, metadata, primary preview, and seven additional preview slots converged. Because publisher code changed, the explicit tooling-validation mode reacquired the exact 15,040,238-byte Workshop package and assertion-tested native Prioritize→cooking→Consume with exact dirty cookware/plate/cutlery return; unchanged gameplay screenshots were not redundantly reviewed. Normal config/prefs hashes matched, product staging was restored, stages/credentials/exact PIDs cleaned, zero RimWorld processes remained, and the exact published candidate was installed locally. Receipt: `artifacts/Releases/fumblesneeze.immersivechefs/publication/20260820T185622796Z/publication-receipt.json`.
- [x] 14.17 COOKWARE WORKFLOW UPDATE: published reviewed source `99ddaca8b93783303fc4d27013fa74c0c7ebda89` and candidate `52888028ECE48DA580F32F50EABDAF0C9574ECD8C04A3BF01E04E37603D6E56A` through immutable plan `52962EEE8A9B4F95371F5B3FC115133CE3A2E018CA046C56EA24146BB388B3EE` to existing public item `3782589902`. Steam change entry `1787303947` retains the exact continuous-dishwasher, Common Sense post-cooking cleanup, and interrupted-reservation note. Remote owner/app/title, Public visibility, `Mod,1.6` tags, Harmony-only dependency, exact plan metadata, 7,968-byte resolved description, primary preview, and seven byte-verified additional preview slots converged; all human-deferred showcases remained excluded. The impact-selected Common Sense native bill and Cook for Yourself workflows passed on the reviewed build at `artifacts/EndToEndRuns/Grouped/20260821T090731130Z`, while reservation cleanup and continuous dishwasher admission retained their separate reviewed live evidence. This ordinary product release did not subscribe, reacquire, move the local product, or launch a post-publication smoke; the receipt records subscriber verification `not-run-for-ordinary-release`, null installed paths/smoke, token cleanup, and exact-process cleanup with zero RimWorld processes remaining. Receipt: `artifacts/Releases/fumblesneeze.immersivechefs/publication/20260821T091821364Z/publication-receipt.json`.
