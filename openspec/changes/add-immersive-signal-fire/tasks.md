## 1. Scaffold and contracts

- [ ] 1.1 Create `mods/ImmersiveSignalFire`, its product csproj, About metadata, release manifest, README, language files, and solution/test-project entries with Harmony and Dynamic Effects Forge hard dependencies and optional Show Me Your Tools metadata.
- [ ] 1.2 Add focused package/source-contract RED tests for owner/package identity, research/building fields, no idle fire/glow/heat comps, dependency classification, localization, assembly allowlist, and strict RGBA asset properties; retain nonzero failing evidence.
- [ ] 1.3 Implement the minimum XML/package structure that makes the scaffold contract GREEN, then run the focused test IDs and `openspec_validate` for both affected changes.

## 2. Contact catalog and menus

- [ ] 2.1 RED: add pure public-behavior tests for alliance, defeat, strict pre-industrial technology, inclusive 10-tile distance, multi-settlement deduplication, nearest/name ordering, and stale revalidation.
- [ ] 2.2 GREEN: implement the bounded faction/settlement contact catalog and the building's pawn float-menu/submenu projection, including the exact no-contact disabled label.
- [ ] 2.3 RED/GREEN: add explicitly owned Harmony shape tests and implement the zero-selected-pawn public post-window `Selector.SelectorOnGUI` capture prefix plus post-`UIRoot_Play.UIRootOnGUI` delivery hook without duplicating selected-pawn options, intercepting input consumed by windows, depending on simulation updates while paused, or affecting other targets.

## 3. Participant dialog and quality

- [ ] 3.1 RED: add pure tests for participant eligibility, locked/inferred caller, two-helper maximum/default selection, distinct interaction cells, Social-as-charisma formula, and strict 15/30/50 outcome boundaries.
- [ ] 3.2 GREEN: implement the participant selection widget and Core-safe `Dialog_BeginSignalFire` composition of the shared begin-lord-job visual vocabulary, with live factors, 600-tick duration, outcome descriptions, and accept-time revalidation.
- [ ] 3.3 REFACTOR: separate engine-free scoring/catalog policy from RimWorld projections while keeping all focused tests green.

## 4. Coordinated jobs, results, and persistence

- [ ] 4.1 RED: add tests for one-to-three synchronized participants, late arrival, interruption, exact 600-tick duration, idempotent terminal resolution, 600–3,600 delayed sampling, and no-resample save data.
- [ ] 4.2 GREEN: implement the building session component, distinct-cell reservations, approach/work job drivers, shared start/end ticks, cancellation, and save/load state.
- [ ] 4.3 RED/GREEN: implement immediate and persisted delayed outcomes through exactly one native-equivalent military-aid projection that retains the worker result, including due-time faction/cooldown revalidation, exact faction mutations, and terminal messages.
- [ ] 4.4 RED/GREEN: implement idempotent terminal destruction and exactly 16 nearby `Filth_Ash` thickness units for success, failure, and active interruption while preserving a gathering-cancelled unused fire.

## 5. Forge effects and optional tool compatibility

- [ ] 5.1 RED: add source/host tests for the exact six-start Morse-like cadence in two groups of three, one unchanged 21-tick original plume and synchronized blanket lift at each start, 39-tick inter-puff and 219-tick inter-group emission silence, mid-puff save/load reconstruction, the original `0.72` emission rate, `0.55~1.15` scale, `0.88`/`0.82` opacity endpoints, reviewed 1.7–2.6 travel range and 0.25–0.75 speed, a Forge-driven product white-alpha steady flame with no stock grayscale source lobes, owned runner cleanup, hard Forge dependency, and no Simple FX dependency.
- [ ] 5.2 GREEN: add product-owned Dynamic Effects Forge flame/smoke Defs and textures, preserve the earlier reviewed smoke-particle appearance, create separated symbol-start emissions, explicitly kill each attached smoke runner at its exclusive 21-tick boundary before Forge can loop it, stop the owned runner at terminal cleanup, and keep the idle building unlit.
- [ ] 5.3 RED/GREEN: add absent, installed-fork, incompatible-shape, and wrong-assembly marker-collision tests for canonical `meathax.showmeyourtools` detection; resolve its marker only from the matched package's assemblies and implement the bounded product blanket renderer driven by the exact owned-runner phase so every blanket lowers when the 21-tick emitter stops.
- [ ] 5.4 Run fresh process-isolated finalized-Def/optional-mod groups for Forge required shape, Show Me Your Tools absent, installed fork present, and deliberately incompatible optional shape; retain results without copying Workshop assemblies.

## 6. Original raster assets

- [ ] 6.1 Measure the read-only old signal fire and at least three Core visual comparators; record hashes, projection/canvas measurements, and an exact written RimWorld brief for a neat wood-only single-use stack with no stones, predominantly pale/green wood, fewer brown pieces, and deliberately simple low-gradient forms before generation.
- [ ] 6.2 Generate at least two original simplified wood-only signal-fire candidates plus required smoke/blanket support art, normalize each to strict 8-bit RGBA, and reject stones, over-detailed/gradient-heavy rendering, clipping, halos, baked backgrounds, or copied reference pixels.
- [ ] 6.3 Build light- and dark-terrain final-map-scale contact sheets, select one candidate with written evidence, package only the selected asset family, and verify exact texture paths/casing.
- [ ] 6.4 Capture the selected idle/active/damaged asset at close, ordinary, and far in-game zoom on materially different terrain; obtain blind context-free independent visual review and resolve every accepted finding.

## 7. Gateway-owned nested menu support

- [ ] 7.1 Complete `add-rimworld-dev-gateway` tasks 36.1–36.3 for exact zero-or-more-actor map-menu opening and replacement-submenu lease transfer with focused RED/GREEN evidence.
- [ ] 7.2 Complete Gateway task 36.4 review/regression and prove the product assembly/package has no Gateway reference.

## 8. Product integration, review, and acceptance

- [ ] 8.1 Add finalized-Def integration coverage for exact research/building/effect/job values and E2E coverage for no-contact, selected-pawn, zero-pawn, three-participant, immediate, delayed, misunderstood, unnoticed, interruption, optional blanket, restored thick smoke plumes with non-emitting spacing, terminal building destruction, gathering cancellation preservation, native active-fire deconstruction through `PostDeSpawn`, and exact bounded soot scenarios.
- [ ] 8.2 Run focused test IDs, owning project build, canonical `mod_build` install, affected integration/E2E groups, strict package validation, and typed OpenSpec validation with nonzero discovery; record durable RED/GREEN/build identities.
- [ ] 8.3 Perform independent scoped code review over the mod and Gateway additions, resolve or explicitly reject every finding with evidence, and rerun every affected test/build/package check.
- [ ] 8.4 Run the final semantic balance/playability pass against measured Core campfire, torch lamp, brazier, comms console, and old signal-fire comparators; update the spec before changing any cost, work, research, soot, threshold, or availability value.
- [ ] 8.5 FINAL IN-GAME ACCEPTANCE: on the reviewed build in fresh minimized isolated exact-mod-list processes, use the native zero-pawn and selected-pawn right-click menus, visible nested faction submenu, shared participant dialog, pawn jobs, synchronized raised-blanket Forge smoke plumes with visible spacing, and optional blankets, then personally observe the causal immediate warrior arrival and representative delayed/failure results plus spent-building destruction and exactly 16-unit soot; retain screenshots, build/package identity, ordered mods, actions, observations, logs, configuration hashes, and exact-PID cleanup before checking this task.
