## 1. `mods/GuestBedGizmo` Foundation and Contracts

- [x] 1.1 Record the read-only RimWorld 1.6 and Hospitality package, assembly, MVID/hash, generated guest-bed Def, Harmony owner, toggle-action, swap-method, translation, and icon contracts in durable local inspection evidence.
- [x] 1.2 Scaffold the `fumblesneeze.guestbedgizmo` product project, honest About/dependency metadata, source layout, and focused test projects without copying optional, Harmony, game, test, host, compiler, or Gateway assemblies.
- [x] 1.3 Register only the new focused host suites/build projects needed by this owning mod while preserving unrelated dirty-worktree changes.

## 2. `mods/GuestBedGizmo` Vertical TDD Activation Slice

- [x] 2.1 RED: add and run focused package-activation tests proving exact canonical Hospitality and Ideology presence selects the supported adapter while either absence does not, retaining nonzero failing results.
- [x] 2.2 GREEN: implement only canonical package activation and rerun the exact focused selection green with a nonzero executed count.
- [x] 2.3 RED: add and run one focused compatibility-shape/legacy-action test against the inspected assembly contract, retaining exact dependency identity and a nonzero failing result.
- [x] 2.4 GREEN: implement the absent-safe exact reflected Hospitality shape probe with one bounded incompatibility warning and rerun that exact selection green.

## 3. `mods/GuestBedGizmo` Vertical TDD Command Slice

- [x] 3.1 RED: add and run one focused four-choice catalog test proving colonist, prisoner, slave, and guest order plus stable translation/icon identities, retaining a nonzero failing result.
- [x] 3.2 GREEN: implement only the native-style four-choice catalog/command metadata and rerun the exact focused selection green with a nonzero executed count.

## 4. `mods/GuestBedGizmo` Vertical TDD Gizmo Projection Slice

- [x] 4.1 RED: add and run one focused Harmony installation/projection test proving idempotent patch ownership and exact legacy-toggle removal without removing unrelated gizmos, retaining a nonzero failing result.
- [x] 4.2 GREEN: implement the after-Hospitality `Building_Bed.GetGizmos` projection that removes only the exact legacy toggle, retains unrelated gizmos, and rerun the exact selection green.

## 5. `mods/GuestBedGizmo` Vertical TDD Conversion Slice

- [x] 5.1 RED: add and run one focused unified-command projection plus selected-bed conversion-plan/replacement-resolution test for guest and vanilla directions, retaining a nonzero failing result.
- [x] 5.2 GREEN: replace/enable the unified owner command and implement synchronous selected-bed guest/vanilla conversion, exact replacement resolution, selection restoration, and delegation to `SetBedOwnerTypeByInterface`; rerun the exact selection green.
- [x] 5.3 Retain durable nonzero RED/GREEN evidence for every exact selection without replaying unrelated suites.
- [x] 5.4 REVIEW TESTS: retain focused host regressions for deferred confirmation planning, crib legacy-toggle preservation, and the exact deferred commit shape; add a live native-menu edge workflow that observes vanilla outdoor-prisoner rejection and mixed animal/foreign owner-command eligibility filtering.
- [x] 5.5 REVIEW GREEN: preserve the legacy toggle without a unified replacement; preflight supported selections; defer guest-to-vanilla swaps until the exact native owner-assignment commit; replace all guest beds in RimWorld's affected closure; and rerun the affected host and exact-loader checks green.
- [x] 5.6 REVIEW RED/GREEN: prove a failed multi-patch publication rolls back before incompatibility is reported, then make the installer remove every owned patch and clear its adapter on any guarded seam failure.

## 6. `mods/GuestBedGizmo` Refactor, Localization, and Package

- [x] 6.1 Refactor while green for one narrow adapter, no optional type leakage, bounded diagnostics, and no speculative settings or persistent state; rerun affected focused tests after each behavior-affecting edit.
- [x] 6.2 Add concise product/Workshop copy and all owned player-facing localization, reusing only Hospitality's active translated guest label/description and validated icon assets.
- [x] 6.3 Build the owning Release project repository-locally with zero warnings/errors and inspect exact package metadata, file/hash inventory, AssemblyRefs, and forbidden artifacts.
- [x] 6.4 Run `openspec validate --all --strict --no-interactive` after the final specification/task edits.

## 7. `mods/GuestBedGizmo` Real Loader and E2E Support

- [x] 7.1 Add a startup-gated integration project for the exact Core/Harmony/Ideology/Hospitality/product/Gateway group that proves finalized guest-bed Defs, the supported shape, one selected strategy, and exact Harmony owner/order; add an inverse absence profile only where it needs real loader evidence.
- [x] 7.2 Add one focused E2E declaration that arranges only disposable beds, drives the actual unified gizmo and float-menu callbacks, captures before/menu/guest/vanilla frames, and asserts exact replacement/conservation/no-duplicate outcomes without directly manufacturing them.
- [x] 7.3 Dry-run the exact integration/E2E staging and runner plans, then run only the new stable IDs needed by this change; personally inspect every exact-run screenshot and classify every relevant log match.

## 8. `mods/GuestBedGizmo` Independent Code Review

- [x] 8.1 Give an independent code reviewer the scoped Guest Bed Gizmo/OpenSpec/test/package diff and requirements, with correctness/tests and KISS/optional-integration focus.
- [x] 8.2 Resolve, reject with evidence, or explicitly defer every finding; rerun every focused test/build/package check affected by accepted fixes.

## 9. `mods/GuestBedGizmo` Final In-Game Acceptance

- [x] 9.1 On the reviewed Release build, run a fresh exact Core/Harmony/Ideology/Hospitality/product/Gateway process, select a real bed, open and observe exactly four choices, choose guests, observe the guest replacement/unified label/retained Hospitality controls/no legacy toggle, choose a vanilla owner type, and observe the reverse conversion.
- [x] 9.2 Personally inspect retained exact-process before/menu/guest/vanilla screenshots at materially different useful zooms and the native developer console/full flushed log; record build/package hashes, mod order, action sequence, causal observations, warnings, request IDs, config hashes, and cleanup.
- [x] 9.3 Run a separate fresh product-only no-Gateway absence workflow, observe the unchanged vanilla bed UI, inspect its log, restore state, gracefully stop the exact PID, and prove both normal configuration hashes match.

## 10. `mods/GuestBedGizmo` Steam Preview and Final Evidence

- [x] 10.1 Before composition, inspect and measure locally installed Workshop/About preview comparators plus the exact live source frames; write the 1164×655, below-1-MiB brief, crop, copy, font, color, corner, page-scale, and rejection criteria.
- [x] 10.2 Add the mod-owned ordered preview manifest/template and deterministic renderer, then generate the single short card from the reviewed in-game four-choice menu frame and inspect full-size and page-scale outputs.
- [x] 10.3 Give an independent blind reviewer only the fresh different-zoom in-game frames and unlabeled final card; require correct bed-owner/guest-purpose identification and no perspective, style, coherence, crop, or zoom-legibility defect.
- [x] 10.4 Pin final source/output hashes and alt text, rerun focused package/presentation checks, complete only evidence-backed task boxes, and report any remaining acceptance blocker precisely.

## 11. `mods/GuestBedGizmo` Public Rename and First Release

- [x] 11.1 Specify and apply the exact public title `Hospitality + Ideoligy Patch` while retaining package ID `fumblesneeze.guestbedgizmo` and runtime assembly identity.
- [x] 11.2 Regenerate and independently review the deterministic renamed Steam/About presentation, then pin its new renderer/output identities.
- [x] 11.3 Add and validate the universal release profile, rebuild/test/package from clean committed source, and prepare the mutation-free private first-publication dry-run.
- [ ] 11.4 Under the user’s standing explicit publication order, pass the prepared digest and nonce directly to publication without another prompt, publish once, persist the Workshop ID, reacquire and verify the subscribed copy, and retain complete release evidence.
